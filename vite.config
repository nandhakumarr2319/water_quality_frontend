import { defineConfig } from "vite";
import react from "@vitejs/plugin-react";

export default defineConfig({
  plugins: [react()],

  server: {
    port: 5173,
    open: true,
    cors: true,
  },

  preview: {
    port: 4173,
  },

  // Vite automatically loads:
  // .env
  // .env.local
  // .env.development
  // .env.production
  //
  // Only variables starting with VITE_ are injected.
});
