import { defineConfig } from "vite";
import react from "@vitejs/plugin-react";

// -------------------------------------------------------
// FINAL WORKING CONFIG FOR BOTH LOCAL + VERCEL PRODUCTION
// -------------------------------------------------------

export default defineConfig({
  plugins: [react()],

  // ⚠️ Do NOT add proxy here — breaks production on Vercel.
  server: {
    port: 5173,
    open: true,
    cors: true,
  },

  preview: {
    port: 4173,
  },

  // IMPORTANT:
  // Vite automatically injects variables starting with "VITE_"
  // Example: import.meta.env.VITE_BACKEND_URL
});
